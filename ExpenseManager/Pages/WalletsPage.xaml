<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ExpenseManager.DTOModels;assembly=ExpenseManager.DTOModels"
             xmlns:viewmodels="clr-namespace:ExpenseManager.ViewModels"
             x:Class="ExpenseManager.Pages.WalletsPage"             
             x:DataType="viewmodels:WalletsViewModel"
             Title="Wallets">
    <CollectionView
        ItemsSource="{Binding Wallets}"
        Margin="10"
        SelectionMode="Single">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout 
                Orientation="Vertical" 
                ItemSpacing="10"/>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate
                x:DataType="models:WalletListDto">
                    <Border
                        Stroke="Green"
                        Padding="10"
                       
                        StrokeShape="RoundRectangle 4">
                            <VerticalStackLayout>
                                <Label 
                                    Style="{StaticResource SubHeadline}"
                                    Text="{Binding Name}">
                                </Label>
                                <Label
                                    HorizontalOptions="Center">
                                    <Label.Text>
                                        <MultiBinding
                                            StringFormat="{} {0} {1}">
                                            <Binding Path="Amount"></Binding>
                                            <Binding Path="Currency" Converter="{StaticResource EnumToDisplayConverter}"></Binding>
                                        </MultiBinding>
                                    </Label.Text>
                                </Label>
                            </VerticalStackLayout>                    
                    </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>