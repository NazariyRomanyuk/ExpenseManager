<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:ExpenseManager.UIModels.ViewModels;assembly=ExpenseManager.UIModels"
             xmlns:pages="clr-namespace:ExpenseManager.Pages"
             x:Class="ExpenseManager.Pages.WalletsPage"
             x:DataType="pages:WalletsPage"
             Title="Wallets">
    <ContentPage.Content>
        <CollectionView
            ItemsSource="{Binding Wallets}"
            Margin="15"
            SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate
                    x:DataType="viewModels:WalletUiViewModel">
                    <Border
                        Stroke="Magenta"
                        Padding="10"
                        Margin="0,10">
                        <VerticalStackLayout>
                            <Label
                                Text="{Binding .}"></Label>
                        </VerticalStackLayout>                    
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>